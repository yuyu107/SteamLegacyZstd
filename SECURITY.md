# 安全软件提示说明

`OldSteam_VSZa_Launcher.exe` 需要在本次运行中对指定旧版 Steam 进程执行临时内存补丁，因此会使用进程枚举、远程加载 DLL、写入进程内存和修改内存页面保护等 Windows API。这些行为会触发部分安全软件的通用“注入/可疑行为”规则。

这不是通过混淆或隐藏行为来规避检测的项目。建议使用者：

1. 只从本仓库的 Release 或可核验来源取得文件。
2. 对照发布页提供的 SHA-256。
3. 自行审阅源码，或按 README 说明自行构建。
4. 如安全软件报毒，将本仓库、源码和对应检测结果一并提交给该安全软件的误报反馈渠道。

程序不会修改硬盘上的 `steam.exe`、`steamclient.dll` 或 `steamui.dll`；Steam 进程退出后临时内存变化自动消失。
